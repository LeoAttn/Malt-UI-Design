extends layout

block style
    link(rel='stylesheet', href='/stylesheets/advanced_search.css')

block content
    div.flex.cats
        each val, index in items
            a
                div.box(style="background-image:url('/images/" + val.name + ".png');")
                h2(style="color:white")= val.name