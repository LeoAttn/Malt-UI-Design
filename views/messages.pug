extends layout

block content
    each message, index in messages
        a.message-block(href="message/" + index)
            div.photo
                i.fas.fa-user
            div.sepa-v
            div.content
                div.name= message.name + ' ' + message.hour
                div.message= message.lastMsg
